# Seamless Trust Engine - KYC demo

Single repo demo including frontend and backend with docker compose.

Services:
- frontend: React guided capture plus liveness demo
- backend: FastAPI service that runs OCR, OpenCV face check, simple risk model, and audit storage

Quick start with Docker Compose:
1. docker compose up
2. Open http://localhost:5173
